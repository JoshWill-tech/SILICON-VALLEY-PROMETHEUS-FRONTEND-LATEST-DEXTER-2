{
  "mcpServers": {
    "magic": {
      "command": "sh",
      "args": [
        "-lc",
        "test -n \"$MAGIC_API_KEY\" || { echo 'MAGIC_API_KEY is not set' >&2; exit 1; }; npx -y @21st-dev/magic@latest -e API_KEY=\"$MAGIC_API_KEY\""
      ]
    }
  }
}
